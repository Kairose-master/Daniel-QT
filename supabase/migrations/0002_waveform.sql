-- ============================================================
-- 응원 음성에 실제 파형(진폭 샘플)을 저장할 컬럼 추가
-- Supabase SQL Editor 에 붙여넣고 Run 하세요. 여러 번 실행해도 안전합니다.
-- ============================================================

-- 0~100 정수 샘플 배열 (녹음 중 수집한 진폭). null 이면 데코용 막대로 표시됩니다.
alter table public.voice_messages
  add column if not exists waveform smallint[];
